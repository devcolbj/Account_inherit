language: python
python:
  - "2.7.15"

# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
#script:
 # - tests
  
script:
  - python -m test -v account_test_users.py
